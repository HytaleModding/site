---
title: Envoyer des notifications
description: Apprenez à envoyer des notifications (similaires à la collecte d'objets) avec votre plugin Hytale
authors:
  - name: BeerBag
---

## Aperçu

Dans ce guide, vous apprendrez comment envoyer des notifications aux joueurs, similaires à
la notification reçue lors de la collecte d'un objet au sol.

![notification-example](/assets/guides/notification-example.png)

## Structure d'une Notification

Une notification se compose de trois composants principaux :

1. **Message Principal** : Le `Message` principal affiché dans la notification.
2. **Message Secondaire** : `Message` supplémentaire, affiché sous le message principal.
3. **Icône** : Une icône d'objet qui représente visuellement la notification, affichée sur
   le côté gauche.

## Envoyer des Notifications

Vous pouvez utiliser la classe `NotificationUtil` pour envoyer des notifications aux joueurs.
Vous aurez besoin d'accéder au `PacketHandler` du joueur, qui peut être obtenu
depuis le `PlayerRef` en utilisant la méthode `getPacketHandler()` (dans cet exemple,
nous obtenons le `PlayerRef` depuis l'`Universe` en utilisant l'UUID du joueur).

```java
public static void onPlayerReady(PlayerReadyEvent event) {
    var player = event.getPlayer();

    var playerRef = Universe.get().getPlayer(player.getUuid());
    var packetHandler = playerRef.getPacketHandler();

    var primaryMessage = Message.raw("THIS WORKS!!!").color("#00FF00");
    var secondaryMessage = Message.raw("This is the secondary message").color("#228B22");
    var icon = new ItemStack("Weapon_Sword_Mithril", 1).toPacket();

    NotificationUtil.sendNotification(
        packetHandler,
        primaryMessage,
        secondaryMessage,
        (ItemWithAllMetadata) icon);
}
```
